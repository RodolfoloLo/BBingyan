# 杂点记录

## zap

zap日志库为了高性能,不会每写一条日志就立刻写入磁盘/控制台,而是先放到内存缓冲区:
问题来了:如果程序突然退出,缓冲区里还没来得及写出的日志就丢失了.
在程序退出前调用zap的Sync方法可以强制将缓冲区里的日志写出,确保日志不丢失.
```go
utils.InitLogger(e)
	// 确保日志缓冲区的日志被写出
	defer utils.Logger.Sync()
```